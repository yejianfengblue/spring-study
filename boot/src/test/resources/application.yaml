spring:
  main:
    banner-mode: "off"

  jpa:
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 10

logging:
  level:
    com.yejianfengblue.spring: debug
